import React from 'react';

import style from './TopBar.scss';

export default () => (
  <div className={style.component}>
    <div className={style.inner}>
      <div className={style.logo}>Drastic</div>
      <div className={style.userInfo}>User Accounts not available yet.</div>
    </div>
  </div>
);
